@using CollegeManagementSystem.Areas.Student.Models
@using CollegeManagementSystem.Models
@model CollegeManagementSystem.Areas.Student.Models.StudentDuty

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Areas/Student/Views/Shared/_StudentLayout.cshtml";
}

<h1>Edit</h1>

<h4>StudentDuty</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Code" />
            <div class="form-group">
                <label asp-for="Description" class="control-label"></label>
                <input asp-for="Description" class="form-control" />
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Status" class="control-label"></label>
                <select asp-for="Status" class="form-control">
                    @foreach (var status in Enum.GetValues(typeof(DutyStatus)))
                    {
                        <option value="@status">@EnumHelper.GetDisplayName((Enum)status)</option>
                    }
                </select>
                <span asp-validation-for="Status" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
